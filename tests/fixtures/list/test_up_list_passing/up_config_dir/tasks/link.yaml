# Should be run as this command should succeed.
run_if_cmd: ["${up_binary_path}", "--version"]

run_lib: "link"

data:
  from_dir: "$link_from_dir"
  to_dir: "$link_to_dir"
  backup_dir: "$link_from_dir/backup"
