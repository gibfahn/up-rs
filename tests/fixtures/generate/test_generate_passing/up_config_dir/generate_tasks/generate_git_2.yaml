run_lib: generate_git

data:
- path: ${root_dir}/up_config_dir/tasks/git_2.yaml
  search_paths: ["${root_dir}/git_scan_dir_2", "${root_dir}/git_scan_dir_3"]
  excludes: ["matches_exclude"]
  prune: false
  remote_order: ["fork", "up"]
